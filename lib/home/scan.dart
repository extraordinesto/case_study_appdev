import 'package:flutter/material.dart';

class ScanProduct extends StatelessWidget {
  const ScanProduct({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: Center(child: Text('Start Scan')));
  }
}
